import React from 'react';
import classes from './EnergyBalance.module.css';

const EnergyBalance: React.FC = () => {
  return <div className={classes.container}>Energy Balance Section</div>;
};

export default EnergyBalance;
